/*
 Crear dos colecciones distintas para poder guardar equipos informáticos (objetos 
 formados por los atributos nombre, cantidad, precio). En una de las colecciones 
 guardaremos los equipos informáticos cuyo precio no tenga descuento y en la otra los mismo equipos pero con descuento.
 Una vez hayamos guardado los objetos en cada estructura pertinente. 
 Debemos mostrar el equipo informático con mayor precio y el de menor precio.
 Comenta que haces para poder lograrlo.
 package entrega;
 */
package entrega;

import java.util.Iterator;
import java.util.Set.*;
import java.util.HashMap;
import java.util.Collection;

public class ejercicioUno {
    public void equipoSinDesc(String nom, Integer cant, float prec) {
        String nombre = nom;
        Integer cantidad = cant;
        float precio = prec;
    }
    HashMap equiposSinDesc = new HashMap();
}
